<br>
<b>@<%= comment.user.first_name %> <%= comment.user.last_name %></b>. <%= comment.commenter %><br>
<small>
    Created: <%= time_ago_in_words(comment.created_at) %>
    <br>
    <% if @current_account.user == comment.user %>
        <%= link_to 'Delete', [comment.post, comment], data: { 'turbo_method': :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
    <% end %>
</small>
<br>
